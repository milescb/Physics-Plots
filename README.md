# Quick Start Guide

1. Install Dependencies with `npm install`
1. Place your PDF files in the `public/plots/` directory:
1. Edit `src/data/plots.json` to match your plots
1. Add any additional pages by copying and modifying the example given from `src/pages/ztautau`. 
1. Run Development Server with `npm run dev`